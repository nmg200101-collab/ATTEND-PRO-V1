<?xml version="1.0" encoding="UTF-8"?>
<lint>
    <!--
      V149 Receiver source is immutable. Android API 33+ annotates the
      three-argument BluetoothGatt.writeCharacteristic result with
      BluetoothStatusCodes. The locked implementation compares SUCCESS to 0,
      which is functionally equivalent but triggers WrongConstant in current lint.
      The V149 byte lock prevents new code from entering this ignored file.
    -->
    <issue id="WrongConstant">
        <ignore path="src/main/java/com/attendpro/store/ReceiverReportBleTransport.kt" />
    </issue>
</lint>
