# دليل المستخدم — الاتصال والربط — ATTEND-PRO RC29

## مركز الاتصال والأجهزة
هذا هو المكان الرئيسي لمتابعة الأجهزة المرتبطة وحالة وجود الموظفين. لا يحتاج مدير المحل إلى قراءة معلومات GATT أو ACK أو HTTP في الاستخدام اليومي؛ هذه التفاصيل تبقى داخل «التشخيص الفني» للصيانة فقط.

## ربط الموظف
يتم ربط تطبيق الموظف بتطبيق المحل باستخدام طرق الربط المعتمدة داخل التطبيق. بعد نجاح الربط لا تحذف بيانات الربط أو تعيد إنشاء الموظف لمجرد تغيير وسيلة الاتصال. التطبيق يستطيع استخدام أكثر من قناة حضور/وجود دون إعادة الاقتران.

## طرق التعرف على وجود الموظف
- Bluetooth: اتصال محلي مباشر. يمكن عرض القرب كتقدير فقط، وليس قياسًا هندسيًا دقيقًا بالمتر.
- Wi‑Fi/LAN: قناة محلية عند وجود الهاتفين على شبكة مناسبة.
- GPS: يحدد هل هاتف الموظف داخل نطاق موقع المحل. GPS مستقل عن Bluetooth بعد استلام إعداد موقع المحل.
- الخادم/الإنترنت: ينقل بيانات GPS والحالات عند توفر الإنترنت، لكنه وحده لا يعني أن جهاز الموظف موجود داخل المحل.

## GPS
من «مركز الاتصال والأجهزة» افتح «تهيئة GPS وتحديد موقع المحل والنطاق»، ثم احفظ موقع المحل ونطاقه. يحتاج هاتف الموظف إلى إذن الموقع المناسب. عند وجود الموظف داخل النطاق يمكن أن يظهر «داخل نطاق المحل» حتى لو كان Bluetooth متوقفًا، بشرط توفر مسار نقل للحالة مثل الإنترنت/الخادم. عند انقطاع الإنترنت يستطيع تطبيق الموظف تصنيف الموقع محليًا باستخدام الإعداد المخزن، ثم يزامن الملاحظة عند عودة الاتصال.

المسافة التي ينتجها GPS هي المسافة بين موقع الهاتف وإحداثيات المحل المحفوظة، وليست المسافة الدقيقة بين هاتفي المحل والموظف. لذلك تعتمد الواجهة على حالة داخل/قرب/خارج النطاق ودقة GPS، بينما قرب Bluetooth — إن ظهر — يظل تقديريًا.

## معنى «جهاز متصل فعليًا»
يُحسب الموظف كجهاز حاضر/متصل عند وجود دليل حديث من Bluetooth أو LAN أو GPS معترف به. اتصال تطبيق المحل بالخادم أو HTTP 200 وحده لا يُحسب جهاز موظف متصل.

## التشخيص الفني
افتح «التشخيص الفني» فقط عند وجود مشكلة. يحتوي على تفاصيل Bluetooth/GATT وACK وLAN والخادم وGPS اللازمة للصيانة. عدم وجود GATT مع استمرار «GPS: داخل نطاق المحل» يعني أن التعرف على الموقع يعمل دون Bluetooth إذا كانت ملاحظة GPS حديثة.

## بعد تحديث التطبيق
منظومة الربط والاتصال في RC29 أساس محمي. تحديثات التقارير أو الإدارة أو اللغة أو الواجهات لا ينبغي أن تطلب إعادة ربط الموظف ولا تغير بروتوكول الاتصال. إذا ظهر عطل ميداني بعد تحديث مستقبلي، سجّل حالة القنوات من «التشخيص الفني» قبل أي إعادة ربط.
